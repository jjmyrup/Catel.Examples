<controls:Page
    x:Class="Catel.Examples.UWP.SavableModel.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Catel.Examples.UWP.SavableModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Catel.Windows.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" HorizontalAlignment="Stretch" Margin="0,0,0,0" VerticalAlignment="Stretch">

    <Grid>
        <StackPanel>
            <TextBox Header="FirstName" Text="{Binding FirstName, Mode=TwoWay}"/>
            <TextBox Header="MiddleName" Text="{Binding MiddleName, Mode=TwoWay}"/>
            <TextBox Header="LastName" Text="{Binding LastName, Mode=TwoWay}"/>
            <Button Content="Save" Command="{Binding SaveCommand}"/>
            <TextBox Header="File path" IsReadOnly="True" Text="{Binding StorageFilePath}"/>
            <TextBlock Text="{Binding Status, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</controls:Page>
